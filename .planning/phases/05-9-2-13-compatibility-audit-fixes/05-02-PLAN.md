---
phase: 05-9-2-13-compatibility-audit-fixes
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/api/page.rs
autonomous: false
requirements:
  - COMPAT-02
tags:
  - compatibility
  - confluence-dc
  - rest-api
  - audit
  - pages
must_haves:
  truths:
    - "Running ccli page list <SPACE-KEY> against the live 9.2.13 instance returns a correctly aligned, non-empty table of pages from that space"
    - "Running ccli page view <PAGE-ID> against 9.2.13 prints the page's storage XML or stripped plain text without parse errors or panics"
    - "Running ccli page create against 9.2.13 successfully creates a new page (returns the created page id and URL)"
    - "Running ccli page edit <PAGE-ID> against 9.2.13 successfully updates the page (version number bumps by 1)"
    - "If any code change was made to src/api/page.rs, all existing tests still pass and at least one new test asserts the 9.2.13 response shape"
  artifacts:
    - path: "src/api/page.rs"
      provides: "list_all_pages, get_page_detail, create_page, update_page, ContentListResponse, PageDetail, PageVersion, PageBody, StorageBody, PageAncestor — possibly with new Option<T> defenses if 9.2.13 differed"
      contains: "fn list_all_pages"
    - path: ".planning/phases/05-9-2-13-compatibility-audit-fixes/05-02-PAGES-FINDINGS.md"
      provides: "Captured 9.2.13 observations for COMPAT-02: status (PASS/FIXED), expected behavior per CRUD op, observed behavior, fix description if any, raw JSON sample if a fix was applied"
      contains: "COMPAT-02"
  key_links:
    - from: "ccli page list / view / create / edit (built from src/cli/page.rs and src/cli/blog.rs)"
      to: "/rest/api/content on the 9.2.13 instance"
      via: "src/api/page.rs::{list_all_pages, get_page_detail, create_page, update_page}"
      pattern: "list_all_pages|get_page_detail|create_page|update_page"
    - from: "src/api/page.rs unit tests"
      to: "httpmock::MockServer with 9.2.13 response shape"
      via: "test_client(server.base_url())"
      pattern: "MockServer::start"
---

<objective>
Audit COMPAT-02 (Confluence Pages endpoints `/rest/api/content` for type=page — list, get, create, update) against the live 9.2.13 instance, fix any breakage in-place in `src/api/page.rs`, and add a unit test for any fix.

Purpose: Pages is the highest-traffic content type in ccli — it covers list/view/create/edit, and has the largest serde struct surface (`expand=body.storage,version,ancestors`) of any endpoint area, so it has the highest probability of revealing 9.2.13 differences in expandable field shapes. Wave 2 because COMPAT-01 (Plan 01) confirmed the test instance is reachable and PAT auth works; this plan does not depend on Plan 01's code changes (file scope is disjoint), only its operational confirmation.
Output: A verified-or-fixed `src/api/page.rs`, plus `05-02-PAGES-FINDINGS.md` for the final TMAT-01 matrix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-9-2-13-compatibility-audit-fixes/05-CONTEXT.md
@.planning/phases/05-9-2-13-compatibility-audit-fixes/05-RESEARCH.md
@.planning/phases/05-9-2-13-compatibility-audit-fixes/05-PATTERNS.md
@src/api/page.rs
@src/api/client.rs
@src/api/error.rs

<interfaces>
<!-- Key types in src/api/page.rs (from PATTERNS.md). Do not re-discover. -->

```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum ContentType { Page, BlogPost }

impl ContentType {
    pub fn as_api_str(&self) -> &'static str { /* "page" | "blogpost" */ }
}

pub struct ContentListResponse {
    pub results: Vec<PageSummary>,
    #[serde(default)] pub _links: ContentListLinks,
    pub start: Option<u64>, pub limit: Option<u64>, pub size: Option<u64>,
}

pub struct PageSummary {
    pub id: String, pub title: String, pub _type: Option<String>,
    pub version: Option<PageVersion>,
    pub ancestors: Option<Vec<PageAncestor>>,
    pub _links: Option<PageLinks>,
}

pub struct PageDetail {
    pub id: String, pub title: String,
    pub version: Option<PageVersion>,
    pub ancestors: Option<Vec<PageAncestor>>,
    pub body: Option<PageBody>,        // PageBody.storage: Option<StorageBody>
    pub _links: Option<PageLinks>,
}

pub struct PageBody { pub storage: Option<StorageBody>, }
pub struct StorageBody { pub value: Option<String>, pub representation: Option<String>, }
pub struct PageVersion { pub number: u32, pub when: Option<String>, ... }

pub async fn list_all_pages(client: &Client, space_key: &str, content_type: ContentType) -> Result<Vec<PageSummary>, AppError>;
pub async fn get_page_detail(client: &Client, page_id: &str) -> Result<PageDetail, AppError>;
pub async fn create_page(client: &Client, space_key: &str, title: &str, body_storage: &str, parent_id: Option<&str>, content_type: ContentType) -> Result<PageDetail, AppError>;
pub async fn update_page(client: &Client, page_id: &str, title: &str, body_storage: &str, current_version: u32, content_type: ContentType) -> Result<PageDetail, AppError>;
```

Endpoints used (from RESEARCH.md table):
- `GET /rest/api/content?spaceKey={K}&type=page&start={S}&limit={L}&expand=version,ancestors` (list_all_pages)
- `GET /rest/api/content/{id}?expand=body.storage,version,ancestors` (get_page_detail)
- `POST /rest/api/content` body: `{type, title, space.key, body.storage, ancestors?}` (create_page)
- `PUT /rest/api/content/{id}` body: `{id, type, title, space.key, version.number, body.storage}` (update_page)

httpmock test pattern: same as plan 01 — local `test_client` helper, `MockServer::start()`, mock with `.method(GET).path(...)` then `.json_body(serde_json::json!({...}))`, call API, assert.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live test ccli page list / view / create / edit against the 9.2.13 instance, capture observations</name>
  <files>None (read-only against live instance + side-effects of `page create` and `page edit` against the test instance)</files>
  <read_first>
    - src/api/page.rs (executor must see PageSummary, PageDetail, ContentListResponse, PageBody, StorageBody, PageVersion, create_page request body shape, update_page version-bump logic)
    - .planning/phases/05-9-2-13-compatibility-audit-fixes/05-RESEARCH.md (sections "Known Differences to Probe" — Probe 1 expand= behavior; Pitfall 2 serde parse errors; Pitfall 3 pagination)
    - .planning/phases/05-9-2-13-compatibility-audit-fixes/05-PATTERNS.md (section "src/api/page.rs" — known risk areas for COMPAT-02/03)
  </read_first>
  <what-built>Existing src/api/page.rs (no changes yet) — ready to test against live 9.2.13.</what-built>
  <how-to-verify>
    Prerequisites: same as Plan 01 (CCLI_URL, CCLI_TOKEN, optional CCLI_INSECURE=1). Plus: pick a known SPACE_KEY where you have create permission and pick a known existing PAGE_ID for view/edit testing.

    Build: `cargo build` (fresh build from after Plan 01 completed).

    Test sequence — record OUTCOME (PASS/FAIL) and OBSERVATIONS for EACH step:

    Step A — List pages in a space:
    ```
    ./target/debug/ccli page list <SPACE_KEY>
    ```
    Expected: Aligned table with columns including ID, Title, Version (or similar). At least one row. No panic.
    Record: full first 5 rows of stdout, exit code, total page count, any stderr.

    Step B — View an existing page (storage XML stripped to plain text by default):
    ```
    ./target/debug/ccli page view <PAGE_ID>
    ```
    Expected: Plain text body printed; no parse errors; exit code 0.
    Record: first 20 lines of output. Note especially if `body.storage.value` was missing or empty.

    Step C — Pagination probe (only if the space has more pages than the default limit):
    ```
    time ./target/debug/ccli page list <SPACE_KEY>
    ```
    Expected: Completes well under 30 seconds; does NOT hang.
    Record: elapsed time. If hangs &gt;60s, kill and note pagination issue.

    Step D — Create a new page (run with EDITOR set to a non-interactive option or use `cat`):
    ```
    EDITOR='/usr/bin/true' ./target/debug/ccli page create --space <SPACE_KEY> --title "compat-9213-test-$(date +%s)"
    ```
    (If your `ccli page create` requires editor interaction, run it once normally, save a minimal storage XML body like `<p>compat 9213 test</p>`, exit editor.)
    Expected: prints created page id and URL; exit code 0.
    Record: created page id (need it for Step E and cleanup), exit code, full stdout.

    Step E — Edit the page just created:
    ```
    EDITOR='/usr/bin/true' ./target/debug/ccli page edit <CREATED_PAGE_ID>
    ```
    (Or open editor, modify body, save.) Expected: returns Ok with new version number; exit code 0.
    Record: any error. Particularly note if a 409 conflict was returned (would indicate the version-number-bump logic broke).

    Step F — Optional cleanup: delete the test page via Confluence UI (out of ccli scope; ccli has no delete command).

    Step G — Optional raw JSON capture (only if any of A–E showed parse errors or unexpected output):
    ```
    curl -sk -H "Authorization: Bearer $CCLI_TOKEN" "$CCLI_URL/rest/api/content?spaceKey=<SPACE_KEY>&type=page&limit=2&expand=version,ancestors"
    curl -sk -H "Authorization: Bearer $CCLI_TOKEN" "$CCLI_URL/rest/api/content/<PAGE_ID>?expand=body.storage,version,ancestors"
    ```
    Record: full JSON; paste into Task 2 findings file.

    Resume signal:
    - "PASS — &lt;notes summarizing all 5 steps worked&gt;"
    - "FAIL on &lt;step letter&gt; — &lt;exact error/observation, plus raw JSON if captured&gt;"
    - If multiple steps failed, list each one explicitly.
  </how-to-verify>
  <acceptance_criteria>
    - User has executed at minimum Steps A and B against the 9.2.13 instance
    - User has typed PASS or FAIL with explicit observations into the resume signal
    - If FAIL: the resume signal explicitly names which sub-step (A/B/C/D/E) failed and includes either the exact error message OR the raw JSON shape captured
    - If a 409 Conflict was returned in Step E: this is recorded with the request body that was sent (visible in tracing logs if RUST_LOG=debug)
  </acceptance_criteria>
  <action>
    Execute the manual test sequence described in &lt;how-to-verify&gt; above: ccli page list / view / create / edit against the live 9.2.13 instance. Record OUTCOME (PASS or FAIL) and OBSERVATIONS for each numbered step verbatim. If any step parse-fails or returns an unexpected response, capture the raw JSON via the curl probe documented in &lt;how-to-verify&gt; and paste it into the resume signal. After completing all steps, type the resume signal exactly as specified in &lt;resume-signal&gt; below.
  </action>
  <verify>
    <automated>MISSING — this is a manual checkpoint task; verification is the human resume signal recorded by the orchestrator. Task 2 of this plan re-runs cargo test and the failed live step (if any) automatically.</automated>
  </verify>
  <done>The user has executed the live test sequence against the 9.2.13 instance and provided a resume signal of either "PASS — &lt;notes&gt;" or "FAIL on &lt;step&gt; — &lt;exact observation, plus raw JSON if captured&gt;". The orchestrator recorded the signal so Task 2 can branch on it.</done>
  <resume-signal>Type "PASS — &lt;notes&gt;" or "FAIL on &lt;step&gt; — &lt;exact error/observation, plus raw JSON if captured&gt;"</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply fix (if FAIL) and write findings file (always)</name>
  <files>
    - src/api/page.rs (modified ONLY if Task 1 returned FAIL)
    - .planning/phases/05-9-2-13-compatibility-audit-fixes/05-02-PAGES-FINDINGS.md (always created)
  </files>
  <read_first>
    - src/api/page.rs (full file — must see all CRUD function bodies, struct definitions, existing #[cfg(test)] mod tests block, the test_client helper)
    - .planning/phases/05-9-2-13-compatibility-audit-fixes/05-PATTERNS.md (section "src/api/page.rs" — Serde struct fields pattern, query() builder, 409 Conflict pattern; "Shared Patterns" — Serde Option Fields rule, D-67 fix-and-update-existing-tests)
    - .planning/phases/05-9-2-13-compatibility-audit-fixes/05-RESEARCH.md (Probe 1 expand= behavior, Pitfall 2 parse errors, Pitfall 4 TUI consumer cascade)
    - src/cli/page.rs and src/tui/screens/pages.rs ONLY IF the fix changes a struct field type — to inspect downstream consumer impact (per Pitfall 4)
  </read_first>
  <behavior>
    Branch on Task 1 outcome:

    If Task 1 returned PASS:
    - No code change. Skip to writing 05-02-PAGES-FINDINGS.md with status PASS.
    - cargo test must still pass (235 existing tests, no new tests).

    If Task 1 returned FAIL on Step A (list parse error):
    - Test 1 (new): list_all_pages parses a 9.2.13-shaped list response (exact JSON from Task 1) and returns Ok with the expected number of pages.
    - If a struct field is now Option, update existing list_all_pages_returns_sorted_vec_on_200 test mock JSON to remain valid.

    If Task 1 returned FAIL on Step B (page view — body.storage shape):
    - Test 1 (new): get_page_detail handles 9.2.13 body shape: e.g., `body: {}` (empty), `body.storage: null`, or `body.storage.value: null`. Asserts Ok with body == None or body.storage == None as appropriate.

    If Task 1 returned FAIL on Step D (create_page — request body rejected):
    - Test 1 (new): create_page sends the request body and parses the 9.2.13 response shape (which may differ from 9.2.19's response). Asserts Ok with the returned PageDetail's id and version.

    If Task 1 returned FAIL on Step E (update_page — 409 conflict on version bump):
    - The current code (PATTERNS.md lines 380-383) maps 409 to `AppError::Api("Conflict: page was updated by someone else (version {})...")`. If 9.2.13's actual update payload requires a different version field name or shape, fix the request body construction and add Test 1: assert update_page sends the corrected body shape via mock with `mock.matches(...)` or `then.expect(1)`.

    If Task 1 returned FAIL on Step C (pagination hang):
    - Same fix pattern as Plan 01 CASE C, applied to the list_all_pages pagination loop. Test asserts loop terminates with mock.expect(1) on the second page.

    All 235 existing tests must continue to pass after the fix (D-67).
  </behavior>
  <action>
    Step 1 — Read Task 1 resume signal.

    Step 2 — Conditional code edit:

    CASE A: Task 1 returned PASS.
      - Do NOT edit src/api/page.rs.
      - Skip to Step 3.

    CASE B: Task 1 FAIL — body.storage absent / different shape.
      - PageBody/StorageBody fields are already Option<T> per PATTERNS.md interfaces. If parse fails despite this, inspect the captured raw JSON: a likely cause is a NEW envelope key wrapping body content. Add a `#[serde(default)] pub <new_key>: Option<<Type>>` to PageDetail or PageBody struct and update the consumer in cli/page.rs `view` handler to read from the appropriate field.
      - Per D-67: if any existing test mock in src/api/page.rs::tests has `body.storage.value: "..."`, leave it (still valid since the field is Option). If a struct field was renamed via `#[serde(rename)]`, update existing mocks to use the canonical (renamed-from) JSON key.

    CASE C: Task 1 FAIL — list parse error.
      - Inspect raw JSON. If a non-Optional field in `PageSummary` is absent in 9.2.13 (e.g., `_type`, `_links`), convert to `Option<T>` with `#[serde(default)]`.
      - Update consumer code in src/cli/page.rs handle_list (existing call sites) to handle the new None case (e.g., `.as_deref().unwrap_or("")`).

    CASE D: Task 1 FAIL — create_page rejected.
      - The request body is constructed as a `serde_json::json!({...})` literal. If 9.2.13 rejected with 400, inspect the response body for the error message (Confluence usually says exactly which field is wrong). Adjust the json!() literal accordingly. Do NOT remove fields that are required on 9.2.19; if both versions need different shapes, this would force a version detection shim — which is forbidden by D-63. In that case, escalate (write findings as FAIL with a note, do NOT add a shim).

    CASE E: Task 1 FAIL — update_page 409.
      - The version-bump logic (`current_version + 1`) and the request body shape are stable across 9.2.x per RESEARCH.md. A 409 likely indicates the page was actually modified between get_page and update_page. If reproducible, capture the exact request body sent (RUST_LOG=debug) and the raw 409 response body, then determine root cause. If 9.2.13 requires a different field name (e.g., `version.number` vs. `version: { number: N }`), adjust the request body. Update existing update_page test mock to assert the corrected body shape.

    CASE F: Task 1 FAIL — pagination hang.
      - Same fix as Plan 01 CASE C, applied to `list_all_pages` loop in src/api/page.rs (the file has its own pagination loop, structurally identical). Replace `let has_next = page.links.next.is_some();` with `let has_next = page.links.next.as_deref().map(|s| !s.is_empty()).unwrap_or(false);`.

    Step 3 — Add unit test (CASES B–F only). Append to existing `#[cfg(test)] mod tests` block in src/api/page.rs:
    ```rust
    #[tokio::test]
    async fn <function_name>_handles_9213_<case_specific_name>() {
        let server = MockServer::start();
        server.mock(|when, then| {
            when.method(<METHOD>).path("/rest/api/content<...>").<query/header constraints>;
            then.status(200).json_body(serde_json::json!({
                // Exact 9.2.13 shape captured in Task 1
            }));
        });
        let client = test_client(&server.base_url());
        let result = <api_function>(&client, ...).await.expect("ok on 9.2.13 shape");
        // Specific assertions
    }
    ```
    Use the existing `test_client` already in src/api/page.rs::tests. For pagination case (F), use `mock.expect(1)` to assert termination.

    Step 4 — cargo build. If a struct field type changed, fix downstream compile errors in src/cli/page.rs, src/cli/blog.rs, and src/tui/screens/pages.rs by adapting consumers to handle the new Option/None case. Per D-64: minimal fix only.

    Step 5 — cargo test. ALL 235 existing tests + new test(s) must pass.

    Step 6 — Re-run the failed live step from Task 1 against 9.2.13. Confirm fix.

    Step 7 — Write `.planning/phases/05-9-2-13-compatibility-audit-fixes/05-02-PAGES-FINDINGS.md`:
    ```markdown
    # COMPAT-02: Pages — Findings

    **Tested:** &lt;DATE&gt;
    **Instance:** Confluence 9.2.13 (live)
    **Status:** PASS | FIXED

    ## Expected Behavior
    `GET /rest/api/content?type=page` lists pages with version+ancestors expansions.
    `GET /rest/api/content/{id}?expand=body.storage,version,ancestors` returns full page detail with storage XML body.
    `POST /rest/api/content` creates a new page; `PUT /rest/api/content/{id}` updates with version bump.

    ## Sub-area Status
    | Sub-area | Status | Notes |
    | -------- | ------ | ----- |
    | List (Step A) | PASS/FIXED | ... |
    | View (Step B) | PASS/FIXED | ... |
    | Create (Step D) | PASS/FIXED | ... |
    | Edit/Update (Step E) | PASS/FIXED | ... |
    | Pagination (Step C) | PASS/N-A/FIXED | ... |

    ## Observed on 9.2.13
    &lt;Verbatim from Task 1&gt;

    ## Fix Applied (if Status=FIXED)
    - File: src/api/page.rs
    - Change: &lt;...&gt;
    - Test added: tests::&lt;test_name&gt; (asserts &lt;...&gt;)
    - Backward compatibility: confirmed via 235-test suite passing post-fix.

    ## Notes for COMPAT-9213.md
    &lt;1–2 sentence note for plan 07's matrix Notes column&gt;
    ```
  </action>
  <verify>
    <automated>cargo test --quiet 2>&amp;1 | tail -5 # MUST show "test result: ok" with at least 235 tests</automated>
  </verify>
  <acceptance_criteria>
    - File .planning/phases/05-9-2-13-compatibility-audit-fixes/05-02-PAGES-FINDINGS.md exists
    - 05-02-PAGES-FINDINGS.md contains "Status: PASS" OR "Status: FIXED"
    - 05-02-PAGES-FINDINGS.md contains the "## Sub-area Status" table with all 5 sub-area rows
    - 05-02-PAGES-FINDINGS.md contains the heading "## Notes for COMPAT-9213.md"
    - cargo test exits 0 with "test result: ok" and total test count ≥ 235
    - If Status=FIXED: `grep -c "9213" src/api/page.rs` returns at least 1
    - If Status=PASS: `git diff --stat src/api/page.rs` shows no changes
  </acceptance_criteria>
  <done>05-02-PAGES-FINDINGS.md exists with PASS or FIXED status; cargo test passes with ≥ 235 tests; src/api/page.rs is unchanged (PASS) or has a backward-compatible fix + new 9213-named test (FIXED); any TUI/CLI compile errors caused by struct changes are resolved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ccli (local) → Confluence DC 9.2.13 | All response data is untrusted external input; serde must defensively parse. |
| ccli `page create` / `page edit` → server | User-supplied storage XML body is forwarded as-is to the server (server is responsible for sanitizing macros/scripts). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Tampering | src/api/page.rs serde parse | mitigate | All struct fields stay `Option<T>`; new fields added via fix MUST also be `Option<T>` with `#[serde(default)]` to prevent parse-failure from a malformed/abbreviated server response. |
| T-05-06 | Denial of Service | list_all_pages pagination loop | mitigate | If CASE F is triggered, the same `_links.next` empty-string guard applied in plan 01 is replicated here. Loop termination asserted with `mock.expect(1)` on second-page mock. |
| T-05-07 | Tampering | update_page version field | mitigate | Existing 409 Conflict handling in src/api/page.rs (PATTERNS.md lines 380-383) returns `AppError::Api("Conflict: page was updated by someone else (version {})...")`, preventing silent overwrites. No change needed unless 9.2.13 requires a different version envelope. |
| T-05-08 | Information Disclosure | RUST_LOG=debug logging during testing | accept | Debug logging is opt-in for diagnosis; reqwest does not log Authorization headers by default. PAT is not leaked. |
| T-05-09 | Tampering | Storage XML body forwarded as-is | accept | Server-side sanitation is Confluence's responsibility; ccli is a thin client per PROJECT.md "raw XML editing" decision. Out of phase scope. |
</threat_model>

<verification>
- `cargo test` passes with at least 235 tests (236+ if a fix was applied).
- `git diff src/api/page.rs` shows either no changes (PASS) or a small backward-compatible diff with new httpmock test (FIXED).
- 05-02-PAGES-FINDINGS.md exists with Sub-area Status table covering all 5 sub-areas.
- The failed live step from Task 1 was re-run after any fix and confirmed working.
</verification>

<success_criteria>
- COMPAT-02 satisfied: `ccli page list / view / create / edit` produce correct output against Confluence 9.2.13 for all 4 CRUD operations.
- Findings captured for plan 07's TMAT-01 matrix.
- D-68 acceptance bar holds.
</success_criteria>

<output>
After completion, create `.planning/phases/05-9-2-13-compatibility-audit-fixes/05-02-SUMMARY.md` with the standard execute-plan summary, including:
- Final status of COMPAT-02 (PASS or FIXED)
- Sub-area breakdown (list/view/create/edit)
- Reference to 05-02-PAGES-FINDINGS.md
- Test count before/after
- Any TUI/CLI compile-error cascades resolved during the fix
</output>
