---
phase: 05-9-2-13-compatibility-audit-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/api/space.rs
autonomous: false
requirements:
  - COMPAT-01
tags:
  - compatibility
  - confluence-dc
  - rest-api
  - audit
must_haves:
  truths:
    - "Running ccli space list against the live 9.2.13 instance prints a non-empty, correctly aligned table of spaces"
    - "Running ccli (TUI) and pressing Enter on a space against 9.2.13 shows a description preview without panicking or showing parse errors"
    - "Pagination terminates correctly when listing spaces on 9.2.13 (does not hang, does not loop)"
    - "If any code change was made, all existing tests in src/api/space.rs still pass and at least one new test asserts the 9.2.13 response shape"
    - "If no code change was needed, the area is marked PASS for COMPAT-9213.md notes (recorded for plan 07)"
  artifacts:
    - path: "src/api/space.rs"
      provides: "list_all_spaces, get_space_detail, SpaceListResponse, SpaceListLinks, SpaceDetail, SpaceDescription, PlainBody — possibly with new Option<T> fields if 9.2.13 differed"
      contains: "fn list_all_spaces"
    - path: ".planning/phases/05-9-2-13-compatibility-audit-fixes/05-01-SPACES-FINDINGS.md"
      provides: "Captured 9.2.13 observations for COMPAT-01: status (PASS/FIXED), expected behavior, observed behavior, fix description if any, raw JSON sample if a fix was applied"
      contains: "COMPAT-01"
  key_links:
    - from: "ccli space list (binary built from src/cli/space.rs)"
      to: "/rest/api/space on the 9.2.13 instance"
      via: "src/api/space.rs::list_all_spaces"
      pattern: "list_all_spaces"
    - from: "src/api/space.rs unit tests"
      to: "httpmock::MockServer with 9.2.13 response shape"
      via: "test_client(server.base_url())"
      pattern: "MockServer::start"
---

<objective>
Audit COMPAT-01 (Confluence Spaces endpoints `/rest/api/space`) against a live Confluence 9.2.13 instance, fix any breakage in-place in `src/api/space.rs`, and add a unit test for any fix using the established httpmock pattern.

Purpose: Spaces is the foundational endpoint area of ccli — the TUI's home screen and the entry point to every other content type. Any breakage here cascades to pages, blog posts, attachments, and comments. This is wave 1 because all downstream waves depend on the assumption that we can list spaces.
Output: A verified-or-fixed `src/api/space.rs`, plus `05-01-SPACES-FINDINGS.md` capturing observed behavior for the final TMAT-01 matrix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-9-2-13-compatibility-audit-fixes/05-CONTEXT.md
@.planning/phases/05-9-2-13-compatibility-audit-fixes/05-RESEARCH.md
@.planning/phases/05-9-2-13-compatibility-audit-fixes/05-PATTERNS.md
@src/api/space.rs
@src/api/client.rs
@src/api/error.rs

<interfaces>
<!-- Key types in src/api/space.rs (from PATTERNS.md). Executor should use these directly. -->

```rust
// src/api/space.rs
pub struct Space { pub key: String, pub name: String, pub _type: Option<String>, ... }

#[derive(Debug, Clone, Deserialize, Default)]
pub struct SpaceListLinks {
    pub next: Option<String>,
    pub base: Option<String>,
}

pub struct SpaceListResponse {
    pub results: Vec<Space>,
    #[serde(default)]
    pub _links: SpaceListLinks,
    pub start: Option<u64>,
    pub limit: Option<u64>,
    pub size: Option<u64>,
}

pub struct SpaceDetail {
    pub key: String,
    pub name: String,
    pub description: Option<SpaceDescription>,
    pub homepage: Option<SpaceHomepage>,
    ...
}

pub struct SpaceDescription { pub plain: Option<PlainBody>, }
pub struct PlainBody { pub value: Option<String>, pub representation: Option<String>, }

pub async fn list_all_spaces(client: &Client) -> Result<Vec<Space>, AppError>;
pub async fn get_space_detail(client: &Client, key: &str) -> Result<SpaceDetail, AppError>;
```

```rust
// src/api/client.rs
impl Client {
    pub fn new(config: &Config) -> Result<Self, AppError>;
    pub fn inner(&self) -> &reqwest::Client;
    pub fn base_url(&self) -> &str;
}
```

Established httpmock test pattern (PATTERNS.md lines 96-124): import `httpmock::prelude::*`, define local `test_client(base_url: &str) -> Client`, use `#[tokio::test]`, build `MockServer::start()`, register `server.mock(|when, then| { when.method(GET).path("/rest/api/space"); then.status(200).json_body(...); });`, call API function with `&server.base_url()`, assert.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live test ccli space list and ccli space TUI against the 9.2.13 instance, capture observations</name>
  <files>None (read-only against live instance; produces console output and notes)</files>
  <read_first>
    - src/api/space.rs (so executor knows exactly what fields are expected: Space, SpaceListLinks, SpaceDetail, SpaceDescription, PlainBody)
    - .planning/phases/05-9-2-13-compatibility-audit-fixes/05-RESEARCH.md (sections "Known Differences to Probe" and "Common Pitfalls" — pre-identified high-probability issues for spaces)
    - .planning/phases/05-9-2-13-compatibility-audit-fixes/05-PATTERNS.md (section "src/api/space.rs" — known risk areas for COMPAT-01)
  </read_first>
  <what-built>Existing src/api/space.rs code (no changes yet) — ready to test against live 9.2.13.</what-built>
  <how-to-verify>
    Prerequisites (user must confirm before executing):
    1. A Confluence 9.2.13 instance is reachable from this machine.
    2. CCLI_URL and CCLI_TOKEN environment variables are exported and point to the 9.2.13 instance and a valid PAT respectively.
    3. If the instance uses a self-signed certificate, CCLI_INSECURE=1 is also exported.

    Build the binary fresh: `cargo build --release` (or `cargo build` for faster turnaround).

    Test sequence — record OUTCOME (PASS/FAIL) and OBSERVATIONS (raw stdout, error messages, behavioral anomalies) for EACH step:

    Step A — Plain table list:
    ```
    ./target/debug/ccli space list
    ```
    Expected: Aligned table of spaces with at least 2 columns (Key, Name); no panic; exit code 0.
    Record: full stdout, total spaces count, exit code, any stderr warnings.

    Step B — Plain CSV-style for piping:
    ```
    ./target/debug/ccli space list --plain --no-headers
    ```
    Expected: Tab-separated rows, no header line, no box-drawing characters.
    Record: first 3 rows verbatim.

    Step C — Pagination probe (only if total spaces > 25):
    ```
    time ./target/debug/ccli space list
    ```
    Expected: Completes in well under 30 seconds; does NOT hang.
    Record: elapsed time. If the command hangs >60s, kill it (Ctrl+C) — this indicates the `_links.next` empty-string sentinel pitfall (PATTERNS.md "Pagination Loop Guard").

    Step D — TUI preview (description.plain probe):
    ```
    ./target/debug/ccli
    ```
    Then: navigate to a space that you know HAS a description, press Enter (or whatever drills down). Verify the description preview renders without "(empty)" placeholder when one was set in the UI. Then navigate to a space that has NO description and verify it does not panic.
    Record: did the description display correctly for the space with a description? Was there a panic or visible parse error for the space without one?

    Step E — Optional: capture raw JSON for a known shape (only if any of A–D produced parse errors or unexpected output):
    ```
    curl -sk -H "Authorization: Bearer $CCLI_TOKEN" "$CCLI_URL/rest/api/space?limit=2"
    curl -sk -H "Authorization: Bearer $CCLI_TOKEN" "$CCLI_URL/rest/api/space/{KNOWN_KEY}?expand=description.plain,homepage"
    ```
    Record: full JSON response, paste into the findings file in Task 2.

    Resume signal:
    - "PASS — no breakage observed" → Task 2 will record PASS and skip the fix
    - "FAIL — <description of breakage>" → Task 2 will fix, then add unit test
    - Always paste any captured raw JSON / error output so Task 2 has concrete data to fix against
  </how-to-verify>
  <acceptance_criteria>
    - User has executed at minimum Steps A and D against the live 9.2.13 instance
    - User has typed PASS or FAIL with explicit observations into the resume signal
    - If FAIL: the resume signal includes the exact error message OR the raw JSON shape that caused the breakage
    - If pagination hung in Step C: this is recorded as a FAIL with note "pagination hang on `_links.next` empty-string sentinel"
  </acceptance_criteria>
  <action>
    Execute the manual test sequence described in &lt;how-to-verify&gt; above: ccli space list and TUI space browse against the live 9.2.13 instance. Record OUTCOME (PASS or FAIL) and OBSERVATIONS for each numbered step verbatim. If any step parse-fails or returns an unexpected response, capture the raw JSON via the curl probe documented in &lt;how-to-verify&gt; and paste it into the resume signal. After completing all steps, type the resume signal exactly as specified in &lt;resume-signal&gt; below.
  </action>
  <verify>
    <automated>MISSING — this is a manual checkpoint task; verification is the human resume signal recorded by the orchestrator. Task 2 of this plan re-runs cargo test and the failed live step (if any) automatically.</automated>
  </verify>
  <done>The user has executed the live test sequence against the 9.2.13 instance and provided a resume signal of either "PASS — &lt;notes&gt;" or "FAIL on &lt;step&gt; — &lt;exact observation, plus raw JSON if captured&gt;". The orchestrator recorded the signal so Task 2 can branch on it.</done>
  <resume-signal>Type "PASS — &lt;notes&gt;" or "FAIL — &lt;exact error/observation, plus raw JSON if captured&gt;"</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply fix (if FAIL) and write findings file (always)</name>
  <files>
    - src/api/space.rs (modified ONLY if Task 1 returned FAIL)
    - .planning/phases/05-9-2-13-compatibility-audit-fixes/05-01-SPACES-FINDINGS.md (always created)
  </files>
  <read_first>
    - src/api/space.rs (full file — must see existing structs, function bodies, and #[cfg(test)] block before touching)
    - .planning/phases/05-9-2-13-compatibility-audit-fixes/05-PATTERNS.md (section "src/api/space.rs" + "Shared Patterns" → httpmock test pattern verbatim, network error mapping, fix-and-update-existing-tests rule D-67)
    - .planning/phases/05-9-2-13-compatibility-audit-fixes/05-RESEARCH.md (Pitfall 2 "Serde parse errors", Pitfall 3 "Pagination loop")
  </read_first>
  <behavior>
    Branch on Task 1 outcome:

    If Task 1 returned PASS:
    - No code change. Skip directly to writing 05-01-SPACES-FINDINGS.md with status PASS.
    - cargo test must still pass (235 existing tests; no new tests added).

    If Task 1 returned FAIL with a description.plain shape difference:
    - Test 1 (new): list_all_spaces handles a space with description=null (full JSON: `{"results": [{"key":"X","name":"X","description": null, ...}], "_links": {}}`) → returns Ok with the space present and `description` field as None.
    - Test 2 (new): get_space_detail handles description={} (empty object) → returns Ok with description.plain == None.

    If Task 1 returned FAIL with a pagination hang (`_links.next` empty string):
    - Test 1 (new): list_all_spaces terminates when a mock returns `{"results": [...], "_links": {"next": ""}}` and a second page returns empty results — loop must break, not infinite-loop. Use mock.expect(1) on the second-page mock to assert at most one extra request is made.

    If Task 1 returned FAIL with another shape (e.g., new field name, different envelope):
    - Test 1 (new): describes the exact 9.2.13 shape captured in Task 1's resume signal, asserts it parses successfully.

    All existing tests in src/api/space.rs (currently passing) must continue to pass after the fix (D-67: update existing mock JSON if the struct shape changed, do NOT add a separate tests_9213 module).
  </behavior>
  <action>
    Step 1 — Read Task 1 resume signal from the orchestrator's record of the human checkpoint.

    Step 2 — Conditional code edit:

    CASE A: Task 1 returned PASS.
      - Do NOT edit src/api/space.rs.
      - Skip to Step 3.

    CASE B: Task 1 returned FAIL — description shape (e.g., `description: null` or `description: {}` caused parse failure or empty render).
      - Inspect the existing structs in src/api/space.rs. The current chain is `description: Option<SpaceDescription>` → `plain: Option<PlainBody>` → `value: Option<String>`. If the parse already succeeds with None, this is a render bug, not a parse bug — root cause may be in src/tui/screens/spaces.rs, but per D-63 scope, only edit src/api/space.rs unless TUI has compile errors after the change.
      - If a NEW field is required (rare): add `#[serde(default)] pub <field>: Option<<Type>>` to the relevant struct.
      - Per D-67: update any existing test mocks in #[cfg(test)] block whose JSON does NOT include the new optional field shape (most tests should still work since the field is optional).

    CASE C: Task 1 returned FAIL — pagination hang (`_links.next == ""`).
      - Edit src/api/space.rs `list_all_spaces` pagination loop. Replace:
        ```rust
        let has_next = page.links.next.is_some();
        ```
        with:
        ```rust
        let has_next = page.links.next.as_deref().map(|s| !s.is_empty()).unwrap_or(false);
        ```
      - This is backward-compatible: `Some("/rest/api/...")` → true (unchanged), `None` → false (unchanged), `Some("")` → false (NEW, fixes 9.2.13).
      - No struct changes, so no existing test updates needed.

    CASE D: Task 1 returned FAIL — other shape:
      - Add `#[serde(default)]` and/or convert to `Option<T>` on the field whose absence/null caused parse failure. Use the captured raw JSON from Task 1 as ground truth for the shape.
      - Update any existing test in src/api/space.rs::tests whose mock JSON contains the now-changed field, to remove the assumption that the field is always present.

    Step 3 — Add unit test (CASES B/C/D only). At the end of the existing #[cfg(test)] mod tests block in src/api/space.rs, append:
    ```rust
    #[tokio::test]
    async fn list_all_spaces_handles_9213_<case_specific_name>() {
        let server = MockServer::start();
        server.mock(|when, then| {
            when.method(GET).path("/rest/api/space");
            then.status(200).json_body(serde_json::json!({
                // Use the exact 9.2.13 shape captured in Task 1
            }));
        });
        let client = test_client(&server.base_url());
        let result = list_all_spaces(&client).await.expect("ok on 9.2.13 shape");
        // Assertions specific to the case
    }
    ```
    Use the existing `test_client` helper already defined in src/api/space.rs::tests; do NOT create a new one. For the pagination case (C), use `mock.expect(1)` to assert termination.

    Step 4 — Run cargo build. If the struct changed shape (CASE B/D), the compiler may flag usages in src/cli/space.rs or src/tui/screens/spaces.rs. Per Pitfall 4 in RESEARCH.md, fix any compile errors by adjusting the consumer to handle the new Option/None case gracefully (e.g., `.as_deref().unwrap_or("")`). Limit scope per D-64: ONLY fix what was broken by the struct change; no other refactoring.

    Step 5 — Run cargo test. ALL 235 existing tests + the new test must pass.

    Step 6 — Re-run the live test (the single failed step from Task 1) against 9.2.13 to confirm the fix works on the live instance. Record observation.

    Step 7 — Write `.planning/phases/05-9-2-13-compatibility-audit-fixes/05-01-SPACES-FINDINGS.md` with this exact structure:
    ```markdown
    # COMPAT-01: Spaces — Findings

    **Tested:** &lt;DATE from `date +%Y-%m-%d`&gt;
    **Instance:** Confluence 9.2.13 (live)
    **Status:** PASS | FIXED

    ## Expected Behavior
    `GET /rest/api/space` returns a paginated list of accessible spaces. `GET /rest/api/space/{key}?expand=description.plain,homepage` returns the space detail with optional description and homepage.

    ## Observed on 9.2.13
    &lt;Verbatim from Task 1: stdout snippets, raw JSON if captured, exact behavior&gt;

    ## Fix Applied (if Status=FIXED)
    - File: `src/api/space.rs`
    - Change: &lt;one-line description, e.g., "Treat empty-string `_links.next` as terminal page (defensive)" or "N/A — PASS"&gt;
    - Test added: `tests::list_all_spaces_handles_9213_&lt;name&gt;` (asserts &lt;...&gt;)
    - Backward compatibility: confirmed via existing 235-test suite passing post-fix.

    ## Notes for COMPAT-9213.md
    &lt;1–2 sentence note that will go into the test matrix Notes column for plan 07&gt;
    ```
  </action>
  <verify>
    <automated>cargo test --quiet 2>&amp;1 | tail -5 # MUST show "test result: ok" with at least 235 tests (236+ if a fix was applied)</automated>
  </verify>
  <acceptance_criteria>
    - File .planning/phases/05-9-2-13-compatibility-audit-fixes/05-01-SPACES-FINDINGS.md exists
    - 05-01-SPACES-FINDINGS.md contains "Status: PASS" OR "Status: FIXED" (one of the two)
    - 05-01-SPACES-FINDINGS.md contains the heading "## Notes for COMPAT-9213.md"
    - cargo test exits 0
    - cargo test output contains "test result: ok"
    - Total test count from `cargo test --quiet 2>&amp;1 | grep -E "^test result" | head -1` is &gt;= 235
    - If Status=FIXED: `grep -c "9213" src/api/space.rs` returns at least 1 (new test name contains "9213")
    - If Status=PASS: `git diff --stat src/api/space.rs` shows no changes
  </acceptance_criteria>
  <done>05-01-SPACES-FINDINGS.md exists with PASS or FIXED status; cargo test passes with at least 235 tests (236+ if fixed); src/api/space.rs is unchanged (PASS) or has a backward-compatible fix + new 9213-named test (FIXED).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ccli (local) → Confluence DC 9.2.13 | All response data is untrusted external input; serde must defensively parse. |
| Test code → httpmock::MockServer | Localhost mock server; trusted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | src/api/space.rs serde parse | mitigate | All response struct fields stay `Option<T>` per Pattern 2; new fields added via fix MUST also be `Option<T>` with `#[serde(default)]` to prevent parse-failure DoS from a malformed/truncated server response. |
| T-05-02 | Denial of Service | list_all_spaces pagination loop | mitigate | If CASE C is triggered, the empty-string-aware `_links.next` guard is added. This prevents an infinite-loop DoS (excessive HTTP requests) caused by a server returning `"_links": {"next": ""}`. Loop termination is asserted with `mock.expect(1)` in the new test. |
| T-05-03 | Information Disclosure | Logging of PAT in error messages | accept | The existing AppError::Auth message is generic ("Authentication failed. Run 'ccli init'..."); no token leakage. No change required. |
| T-05-04 | Tampering | New httpmock test JSON | accept | Test data is hand-written by the implementer; not from external source. |
</threat_model>

<verification>
- `cargo test` passes with at least 235 tests (236+ if a code fix was applied).
- `git diff src/api/space.rs` shows either no changes (PASS) or a small, backward-compatible diff with a new `httpmock`-style test (FIXED).
- 05-01-SPACES-FINDINGS.md exists and follows the required heading structure.
- The live test from Task 1 was re-run after any fix and confirmed working against 9.2.13 (recorded in findings file).
</verification>

<success_criteria>
- COMPAT-01 requirement is satisfied: `ccli space list` and TUI space browsing produce correct, non-panicking output against Confluence 9.2.13.
- Whatever was observed (PASS or FAIL→FIXED) is captured in 05-01-SPACES-FINDINGS.md with raw observations, ready to be transcribed into COMPAT-9213.md by plan 07.
- D-68 acceptance bar holds: all 235 existing tests pass; if a fix was applied, at least one new test exists.
</success_criteria>

<output>
After completion, create `.planning/phases/05-9-2-13-compatibility-audit-fixes/05-01-SUMMARY.md` with the standard execute-plan summary template, including:
- Final status of COMPAT-01 (PASS or FIXED)
- Reference to 05-01-SPACES-FINDINGS.md
- Test count before/after (235 → 235 or 236+)
- Any compile-error follow-ups in src/cli/ or src/tui/ caused by the fix (if any)
</output>
